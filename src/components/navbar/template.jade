nav.navbar.blue-grey.z-depth-1
  div.nav-wrapper
    ul
      li(v-for="tab in tabs" v-on:click="selectTab(tab.id)")
        a.blue-text.text-lighten-4(v-if="tab.enabled") {{tab.name}}
        s(v-else="tab.enabled")
          a.blue-text.text-lighten-4 {{tab.name}}
