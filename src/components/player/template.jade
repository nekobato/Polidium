video.video-player(
  v-if='playMode === "video"'
  autoplay
  v-bind='{ controls: player.controls, src: player.src }'
)
iframe.web-player(
  v-if='playMode === "web"'
  v-bind='{ src: player.src }'
)
